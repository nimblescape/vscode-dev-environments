# Container restrictions

This document lists every restriction that Dev Environments applies to its dev containers, grouped by **who normally configures** the thing that the extension restricts. The requirements are in the concept ([vscode-dev-environments.md](vscode-dev-environments.md), section 9 "Host access" and "Accounts"); the technical decisions are in the [implementation notes](implementation-notes.md).

**Principle.** Nothing that configures the container may reach the computer, with three exceptions that the user decided to keep: the network (including VPN connections of the computer), ports of the container on `localhost` of the computer, and URLs that open in the computer's browser.

**Kinds of restriction.**

| Kind | Meaning |
|---|---|
| Refused | Start stops with a message that names each offending setting, before anything is pulled, built, or started. The workspace volume is kept. |
| Rewritten / removed | The extension changes the value in the override configuration that it gives to `devcontainer up`. |
| Neutralized | A channel still exists, but the extension makes the container ignore it (environment variables, files in the container). |
| Not passed | A value of the computer does not reach the workspace helper or the container. |
| Kept | The extension leaves it as it is (user decision); see the known limits (section 10). |

**Status.** Everything in this summary is implemented.

## 1. Repository author (`devcontainer.json`)

| What they normally configure | What the extension does | Kind |
|---|---|---|
| Bind mounts in `mounts` (string or object form). The Dev Container CLI turns each entry into `--mount` of `docker run`. | Every source that is a path of the computer is refused: it contains `/` or `\`, or starts with `.`, `~`, or a drive letter. This includes the Docker socket and `${localWorkspaceFolder}/…`. A text that Docker would parse differently is refused as a whole. | Refused |
| Other mount types in `mounts` | Named, anonymous, and tmpfs mounts are allowed. Refused: `volume-driver` and `volume-opt` (they can bind a folder of the computer) and `volume-label`, the types npipe, image, cluster, and unknown types, and named volumes that belong to something else: a volume named like an anonymous volume (64 hexadecimal characters, an anonymous volume of another container), the workspace volume of another environment (named `devenv-<name>-<8 hexadecimal characters>`; other `devenv-*` volumes of a repository are allowed), a volume that an environment of another GitHub account or an entry of an older version without owner uses (its additional volumes in the registry: the named volumes of its configuration, its merged configuration, and the image metadata of its base image and Features, read with the parser of the policy, and the named volumes that its container mounts (a name with `${devcontainerId}` is resolved only when `up` creates the container); a recorded volume stays recorded, and when a Delete or a failed first open keeps it, the registry keeps its name with the account while the volume exists), the helper's cache volume `devenv-helper-cache`, the volumes of the Dev Containers extension (`vscode`, its cache of VS Code Server for its dev containers; `vsc-remote-containers` and names that end in a hexadecimal MD5 or SHA-256 hash, its clones of repositories), and an existing volume whose labels show that another program created it: Docker Compose (`com.docker.compose.*`, for example the data of a database), the Dev Containers extension (`vsch.*`, `dev.container.volume`), Docker (`com.docker.volume.anonymous`, an anonymous volume of another container), or Dev Environments (`devenv.environment-id`). The extension reads the labels of the named volumes of the mounts before each check. Options of a volume other than `volume-nocopy` and `volume-subpath` are refused too, also `volume-label`: a mount could otherwise give a volume that it creates the labels by which the extension restores environments after a lost registry (the restore also accepts only a volume with the name that the extension gives the environment of its labels). So a volume that a configuration creates by its mounts has no labels, and the volumes of the repository (for example `${localWorkspaceFolderBasename}-node_modules`) stay allowed, for the environments of the account that uses them first: another account's environment of the repository is refused them (known gap 4). | Refused |
| Mounts in `runArgs` (`-v`, `--volume`, `--mount`, in every spelling). The CLI appends `runArgs` unchanged to `docker run`. | The same rules as for `mounts`. | Refused |
| Devices, namespaces, and container sharing in `runArgs` | Refused: `--device`, `--device-cgroup-rule`, `--gpus`, and the limits that name devices (`--device-read-bps`, `--device-write-bps`, `--device-read-iops`, `--device-write-iops`, `--blkio-weight-device`); `--runtime` (another runtime can add devices), `--volume-driver`, `--use-api-socket` (the Docker socket and the registry credentials), `--cgroup-parent`, `--oom-kill-disable`; `--pid`, `--ipc`, `--uts`, `--userns`, `--cgroupns` with any value; `--volumes-from` and `--link`; `--network`/`--net container:<name>`, also in Docker's long form `name=container:<name>[,…]` (it joins another container's network, for example another environment's services). | Refused |
| Labels in `runArgs` (`--label`, `-l`) | Refused when the key starts with `devenv.` or `devcontainer.` (compared without case and surrounding spaces), with the message "options that Dev Environments does not support". The Dev Container CLI puts its id label `devenv.environment-id` before the `runArgs` in `docker run`, and Docker keeps the last label of a key: the extension and the Session Monitor would no longer find the container, or take it for another environment. The CLI adds `devcontainer.metadata` after the `runArgs`, but the policy does not rely on this order. Allowed: exactly the two labels that the override configuration adds itself, `devenv.container-version=3` and `devenv.container-config=unknown`. | Refused |
| Environment variables of container-only Git: `-e`/`--env` in `runArgs`, `containerEnv`, and `remoteEnv` | Refused, with or without a value (compared without case): the variables that the override configuration sets (`GIT_CONFIG_GLOBAL`, `GIT_CONFIG_COUNT`, `GIT_CONFIG_KEY_n`, `GIT_CONFIG_VALUE_n`, `DOCKER_CONFIG`, `GIT_SSH_COMMAND`, `GH_CONFIG_DIR`) and every other variable of Git's configuration (`GIT_CONFIG`, `GIT_CONFIG_*`, for example `GIT_CONFIG_PARAMETERS`, which Git applies after `GIT_CONFIG_COUNT`). In `docker run`, a `-e` of `runArgs` comes after the `containerEnv` of the override configuration and replaces its value for the main process of the container and for `docker exec`; a `-e NAME` without a value removes it (checked with Docker 29.8). `containerEnv` and `remoteEnv` of the repository would be replaced by the values of the override configuration without a word, or would change Git's configuration. The same variables of Features and the base image are refused on the environment image before each container creation. The variables of the Dev Containers extension, the VS Code server, and GnuPG (for example `SSH_AUTH_SOCK`, `REMOTE_CONTAINERS_IPC`, `GNUPGHOME`) are neither set nor refused. `--env-file` stays allowed below `/workspaces`; its content is not checked (see section 10). | Refused |
| VS Code settings of the configuration (`customizations.vscode.settings` of the repository, a Feature, or the base image) | `remote.localPortHost` with a value other than `localhost` is refused: the Dev Containers extension writes these settings into the settings of the container, the window applies them, and VS Code would forward the ports of the container on all addresses of the computer. The setting of the user stays the user's choice. Other settings are not checked. | Refused |
| Any other `runArgs` flag | An allow-list of flags that give no access to the computer, each with its reason in the code (`RUN_FLAGS` in `src/core/helper/hostAccess.ts`): the network (`--network`, `--add-host`, `--dns*`, `--ip`, `--ip6`, `--network-alias`, `--expose`); names and variables (`--label`/`-l` and `--env`/`-e` except those above, `--hostname`, `--env-file` only for a file below `/workspaces` without `..`, because `docker run` reads it in the workspace helper); limits (`--memory*`/`-m`, `--cpus`, `--cpu-shares`/`-c`, `--cpu-period`, `--cpu-quota`, `--cpu-rt-period`, `--cpu-rt-runtime`, `--cpuset-cpus`, `--cpuset-mems`, `--blkio-weight`, `--pids-limit`, `--shm-size`, `--ulimit`, `--storage-opt size=…`, `--oom-score-adj` of 0 or more); the user (`--user`, `--group-add`, `--workdir`); `--init`, `--platform`, `--tmpfs`, `--isolation`, `--sysctl` (Docker accepts only settings of the container's own namespaces); flags that only take rights away (`--cap-drop` with any value, `--read-only`, `--security-opt no-new-privileges`); the health check (`--health-*`, `--no-healthcheck`); the stop (`--stop-signal`, `--stop-timeout` up to 20 seconds, `--restart no` or `on-failure[:n]`); logs in files of the container (`--log-driver json-file`, `local`, or `none`, and `--log-opt` of the size, rotation, mode, and content of the log); flags without effect (`--entrypoint` and `--attach`/`-a`: the CLI sets its own); `--cap-add SYS_PTRACE`, `--security-opt seccomp=unconfined`, and `-p` on a loopback address. Everything else is refused, including stray arguments, entries that are not strings, a flag without its value at the end of the list, and groups of short flags that contain a flag that is not known or that takes a value. Flags and values that the policy does not support get a message of their own ("options that Dev Environments does not support"), for example `--restart=always` (it starts the container together with Docker, outside the Session Monitor) and `--stop-timeout=60` (longer than a stop of the Session Monitor may take). A negative `--oom-score-adj` and other log drivers count as access to the computer. The final `runArgs` of the override configuration are checked again before `up`. | Refused |
| `--rm`, `-i`, `-t`, `--interactive`, `--tty`, `-d`, `--detach` in `runArgs`, also in groups such as `-it`, `-ti`, or `-dit` | Allowed, but removed from the `runArgs` of the override configuration, read with the same flag parser as the check (a flag that is the value of another flag stays), and named in the log with the reason. `--rm`: the extension manages the container life cycle (stop, start, recreation), and Docker would delete the container at each stop. `-i` and `-t`: the Dev Container CLI in the workspace helper has no terminal, so `-it` makes the start fail ("stdin is not a terminal"); alone, they do nothing for a dev container. `-d`: the CLI stays attached to the container, and Docker refuses `-d` together with the CLI's `-a`. | Removed |
| `--name` in `runArgs` (without it, Docker picks a random name) | Removed, read with the same flag parser as the check (a `--name` that is the value of another flag stays), and named in the log; the extension adds `--name devenv-<owner>-<repository>-<short id>` at the end. | Removed |
| Published ports with an address (`appPort` strings, `-p`/`--publish`) | Every address outside `127.0.0.0/8` and `::1` is refused (for example `0.0.0.0`, `[::]`, `localhost`), and so are `-P`/`--publish-all`, `appPort` entries that are neither a number nor a string, and Docker's long syntax (`published=8080,target=80`, any value with `=`), which has no address, so Docker would publish it on all addresses. | Refused |
| `build.options` (the CLI appends them unchanged to `docker build`) | Refused: `--secret`, `--ssh`, `--allow`, `--output`/`-o`, `--build-context` except `docker-image://` and `http(s)://`, stray arguments, unknown options. Allowed: `--network` (also `host`), `--add-host`, `--build-arg`, `--target`, `--label`, `--platform`, `--pull`, `--no-cache`. | Refused |
| `hostRequirements.gpu` (Dev Containers turns it into `--gpus all`) | Every value other than `false`, `null`, or absent is refused. The cpus, memory, and storage requirements are neither checked nor applied. | Refused |
| `initializeCommand` (Dev Containers runs it on the computer) | Refused (any non-empty value) and never passed to `up`: in the workspace helper it would run with the Docker socket. | Refused |
| `dockerComposeFile` | Refused: "Docker Compose configurations are not supported yet." | Refused |
| `workspaceMount` and `workspaceFolder` | Always the environment's named volume at `/workspaces`, with the folder `/workspaces/<repository>`; the repository's values are ignored. | Rewritten |
| `${localEnv:NAME}` and `${env:NAME}` (values of the computer) | Not passed. They resolve to their default, to an empty value, or to the helper's own value (`HOME=/root`, `PATH`, `HOSTNAME`, `NODE_VERSION`, `YARN_VERSION`). One warning names them, and those that get the helper's value. | Not passed |

## 2. Dev Container Feature author (`devcontainer-feature.json`)

| What they normally configure | What the extension does | Kind |
|---|---|---|
| `privileged` (for example docker-in-docker). It can also come from `devcontainer.json`, `runArgs`, or the image label; the CLI turns it on if any source sets it. | Refused for every value that counts as true, and `--privileged` with any value. | Refused |
| `capAdd` (the CLI adds the union of all sources as `--cap-add`) | Every capability except `SYS_PTRACE` is refused. | Refused |
| `securityOpt` (the CLI adds the union of all sources as `--security-opt`) | Every option except `seccomp=unconfined` and `no-new-privileges` (also with `=` or `:` and `true` or `false`) is refused. | Refused |
| `mounts` of a Feature (for example docker-outside-of-docker binds `/var/run/docker.sock`), `hostRequirements.gpu`, `initializeCommand`, `remote.localPortHost` in `customizations.vscode.settings` | Checked in the merged configuration (`devcontainer read-configuration --include-merged-configuration`) before any pull, build, or start. If the merged read fails (offline, or a private base image), only the repository configuration is checked before the build; the check of the environment image before `up` still catches the Features, but only after their install scripts ran. Install scripts and entrypoints are not restricted. | Refused |
| `containerEnv` and `remoteEnv` of a Feature (or of the base image) with a variable of container-only Git (section 1) | Refused on the environment image before each container creation (not in the merged configuration, which for an existing container holds the values of the override configuration, also of an earlier version of the extension). | Refused |

## 3. Base image (`devcontainer.metadata` label)

| What they normally configure | What the extension does | Kind |
|---|---|---|
| The label `devcontainer.metadata` of the image in `image` or in the Dockerfile's `FROM` (for example prebuilt images with Features) | `mounts`, `privileged`, `capAdd`, `securityOpt`, `hostRequirements.gpu`, `initializeCommand`, and `remote.localPortHost` in `customizations.vscode.settings` are checked through the merged configuration before the build, and on the environment image before each container creation. `runArgs`, `appPort`, and build options in the label are ignored, as the CLI ignores them. | Refused |

## 4. Dev Container CLI

| What it normally does | What the extension does | Kind |
|---|---|---|
| Mounts the workspace as a bind mount of the local folder | The workspace is always the environment's named volume (see section 1). | Rewritten |
| Reuses the container that its id labels find | The extension adds `--label devenv.container-version=3` (3: the settings of the Dev Containers extension for the container, and only the documented variables of Git and Docker, see section 5). A container without this label, or with an older value, is created again from its environment image (`up --remove-existing-container`, no build, the volume is kept). This happens once; data outside `/workspaces` is lost (the progress says so) and `onCreateCommand`/`postCreateCommand` run again. A container created while the configuration could not be read gets `--label devenv.container-config=unknown` instead of the repository's `runArgs` and `appPort`, and is created again once the configuration can be read. | Rewritten |
| Updates the UID of the remote user (`--update-remote-user-uid-default on`) | The extension passes `never`; the ownership fix sets the file owners instead. | Rewritten |
| Downloads Features with the credentials of the machine where it runs (`DEVCONTAINERS_OCI_AUTH`, Docker config, `GITHUB_TOKEN`) | None of them reach the workspace helper: private Features cannot be downloaded. | Not passed |

## 5. Dev Containers extension (Microsoft)

These are channels that the Dev Containers extension opens when it attaches a window, controlled by its settings (defaults in brackets).

| What it normally does | What the extension does | Kind |
|---|---|---|
| Copies the computer's `~/.gitconfig` and `~/.config/git/config` into the container (`dev.containers.copyGitConfig`, on) | The override configuration sets the documented setting `copyGitConfig` to `false` for the container, under `dev.containers.copyGitConfig` and `remote.containers.copyGitConfig` (`customizations.vscode.settings`; the Dev Containers extension reads the new key and falls back to the old one for a `false`, so both are needed). `GIT_CONFIG_GLOBAL=/workspaces/.devenv+/gitconfig` (containerEnv and remoteEnv), so Git 2.32 or newer reads no home file. Before the first attach, the extension writes a `~/.gitconfig` with an `[include]` of the volume's configuration when the image has none or an empty one, for Git older than 2.32 and for processes without the variables; a file of the image with content stays as it is. | Rewritten |
| Forwards Git credential requests to the computer's credential helper (`dev.containers.gitCredentialHelperConfigLocation`, global) | The container setting `dev.containers.gitCredentialHelperConfigLocation: none`: the forwarding helper is not configured. Also, four Git settings of the command line level (`GIT_CONFIG_COUNT=4`, Git 2.31 or newer): `credential.helper=` (clears every helper of the files), an include of the user's `/workspaces/.devenv+/credentials.gitconfig` (helpers for other hosts), then for `https://github.com` a clear again and the container's own helper. Older Git gets the same through the `~/.gitconfig` of the extension; the extension warns about Git older than 2.9 after each container creation. | Rewritten |
| Git uses the computer's GitHub credentials | Git uses only the owner account's token in `/workspaces/.devenv+/github-token` (mode 0600, owned by the remote user), written at every open by a helper run without Docker socket and network. The credential helper answers only `get` requests for `https://github.com`. No `GH_TOKEN`, no `GITHUB_TOKEN`. | Rewritten |
| Writes a Docker credential store that forwards to the computer (`dev.containers.dockerCredentialHelper`, on) | The container setting `dev.containers.dockerCredentialHelper: false`: no helper program `/usr/local/bin/docker-credential-dev-containers-*` and no `credsStore`. Also `DOCKER_CONFIG=/workspaces/.devenv+/docker` (an empty folder); tools other than the Docker CLI may ignore it. | Rewritten |
| Signs in the GitHub CLI of the container with the computer's `gh` token (`dev.containers.githubCLILoginWithToken`) | The container setting `dev.containers.githubCLILoginWithToken: false`. The GitHub CLI reads its configuration from `/workspaces/.devenv+/gh` (`GH_CONFIG_DIR`), where the extension signs it in at every open with the VS Code GitHub sign-in of the account that owns the environment (`hosts.yml`: the same token as `github-token`, mode 0600). Nobody signs in to `gh` in the container; a sign-in there is replaced at the next open. | Rewritten |
| Gives the VS Code server and its terminals `REMOTE_CONTAINERS_IPC`, the path of its socket `/tmp/vscode-remote-containers-ipc-<id>.sock`. The socket answers only the requests of its forwarding credential helpers, with the computer's Git credentials (`git credential fill`, `approve`, `reject` on the computer) and Docker credentials (`get`, `list`, `store`, `erase`). | Not changed: the variable keeps the value of the Dev Containers extension (user decision: no variable of the Dev Containers extension or the VS Code server is set or changed). The settings of the container switch its credential helpers off (the rows above), so Git and Docker in the container do not use the socket. Opening URLs does not use it either: `$BROWSER` and the `code` command use the command-line socket of the VS Code server (`VSCODE_IPC_HOOK_CLI`), `openExternal` the connection of the extension host, and Open in Browser runs in the window. The socket stays usable by a process that looks for it (section 10). Checked against Dev Containers 0.470.0 and VS Code 1.139; to verify in real VS Code (V-8). | Kept |
| Forwards the GPG agent when the container has no private keys | Not changed (user decision: no internal behavior of the Dev Containers extension is used to switch a feature off, and there is no setting for it). Git in the container signs nothing unless the user configures it. The forwarded agent socket stays usable by a process that looks for it (section 10). A folder `/workspaces/.devenv+/gnupg` of an earlier version stays unused. | Kept |
| Forwards the SSH agent (`SSH_AUTH_SOCK=/tmp/vscode-ssh-auth-<id>.sock`) | `GIT_SSH_COMMAND='ssh -o IdentityAgent=none'` (containerEnv and remoteEnv): Git over SSH does not use the agent. `SSH_AUTH_SOCK` keeps the value of the Dev Containers extension (user decision), so other programs, for example `ssh` in a terminal, can use the agent of the computer (section 10). | Rewritten (Git) / kept |
| Shows every container in Remote Explorer and "Attach to Running Container", whichever account is signed in | The extension shows and allows only the environments of the signed-in GitHub account (sidebar, search, switcher, pickers, reopen at startup); signed out, none. The owner is stored in the registry and in the volume label `devenv.owner-id`. | Removed |
| Creates a new volume and container per "Clone Repository in Container Volume" | One environment per repository and GitHub account: the first Start of an account creates its own environment (clone, volume, container, Git identity, token), also when another account has an environment of the repository. | Refused / rewritten |
| Gives the bootstrap container the Docker socket | Only the Dev Container CLI runs of the workspace helper (read-configuration, build, up) get the socket and the cache volume; Git runs and file reads get neither, and runs that need no network get `--network none`. The helper gets no environment variables of the computer. | Not passed |
| Stops the container when the window closes (`shutdownAction`, stopContainer) | The override sets `shutdownAction: none`; the Session Monitor stops containers that no window uses. A repository's `none` is kept only with `devEnvLauncher.respectShutdownActionNone`. | Rewritten |

## 6. Docker defaults

| What Docker normally does | What the extension does | Kind |
|---|---|---|
| Publishes ports without an address on all interfaces | Bound to `127.0.0.1`: `8080:80` becomes `127.0.0.1:8080:80`, `80` becomes `127.0.0.1::80`, a number `n` becomes `127.0.0.1:n:n`. | Rewritten |
| Lets every process of the user control every container | Start, Stop, Delete, Rebuild, Switch branch, Select configuration, Reconnect, the switcher, and the open refuse another account's environment. | Refused |
| Runs a container until it is stopped | The Session Monitor stops unused containers after `devEnvLauncher.waitingTimeSeconds` (default 30 s). | Rewritten |

## 7. VS Code

| What VS Code normally does | What the extension does | Kind |
|---|---|---|
| Restores and attaches windows (`window.restoreWindows`) | A window that is restored, reloaded, or attached to another account's container runs no pipeline and closes its connection; a sign-in, sign-out, or account switch closes such a window at once and removes the token (and the sign-in of the GitHub CLI) from the volume of the environment, whether its container runs or not. A window that keeps its connection is closed again about every 10 s; it reloads when the owner signs in again. A window attached to a container of an older extension version that the pipeline cannot create again closes its connection too. | Refused |
| Workspace Trust before opening code | The first open of a repository whose owner is not the account or one of its organizations needs a confirmation. | Refused unless confirmed |

## 8. The computer's environment and settings

| What normally applies | What the extension does | Kind |
|---|---|---|
| Values for `${localEnv:…}` from the VS Code process | See section 1. | Not passed |
| Dev Containers settings for container creation (`defaultFeatures`, `gpuAvailability`, `workspaceMountConsistency`, `mountWaylandSocket`, `cacheVolume`) | Do not apply: the CLI in the workspace helper creates the container; the Dev Containers extension only attaches. Of the settings used at attach time, copyGitConfig, the credential helpers of Git and Docker, and the sign-in of the GitHub CLI are switched off for the container (section 5); dotfiles and defaultExtensions still apply. | Not passed |

## 9. Deliberately kept

| What | Normally configured by | Note |
|---|---|---|
| Full network: internet, LAN, VPN, and services on the computer through `host.docker.internal`; `--network host`, `--add-host`, `--dns*`, `--ip`, `--network-alias`, and build `--network host` | Docker defaults; repository `runArgs`/`build.options` | Only `--network container:<name>` is refused, also in the long form `name=container:<name>`. VPN reachability on Windows (WSL 2) and Linux: to verify (V-7). |
| With `--network host` (and macvlan/ipvlan), published ports are ignored and container ports become ports of the computer | Repository author; Docker Desktop host networking | An exception to "ports on localhost only". On Linux with Docker Engine, `--network host` also shares the abstract Unix sockets of the computer (section 10). |
| VS Code port forwarding to `localhost` of the computer | VS Code (`remote.localPortHost`, default localhost) | With `allInterfaces` in the settings of the user, forwarded ports listen on all interfaces. The configuration may not set it (section 1); an image or a process in the container that writes the machine settings file of VS Code Server can (section 10). |
| URLs open in the computer's browser (`$BROWSER`, `openExternal`, Open in Browser) | VS Code | Any process in the container can open any URL. They use the channels of VS Code itself, not `REMOTE_CONTAINERS_IPC` (section 5). |
| The owner's GitHub token (scopes `repo`, `read:org`), readable by every process in the container | VS Code GitHub session | User decision (like a codespace, but a wider scope than a codespace token). |
| The computer's registry credentials for the image check and the pull on the computer; for ghcr.io, the GitHub session with `read:packages` | The user (`~/.docker/config.json`, credential helpers) | Used on the computer only; they never enter the helper or the container. |

## 10. Known limits

- **Same macOS user.** The account separation protects against using the wrong GitHub account. It does not protect against another person or process working as the same macOS user: `docker exec`, Docker Desktop, and the Dev Containers commands reach every environment, including another account's token file. The registry, the reopen record, pending operations, and the Session Monitor are shared by all accounts. The remedy is separate users on the computer.
- **Channels of the Dev Containers extension and VS Code.** The IPC socket of the Dev Containers extension with its client script (`/tmp/vscode-remote-containers-ipc-<id>.sock`, `/tmp/vscode-remote-containers-<id>.js`), the SSH agent socket (`/tmp/vscode-ssh-auth-<id>.sock`), and the GPG agent socket exist in every attached container, and their variables (`REMOTE_CONTAINERS_IPC`, `SSH_AUTH_SOCK`) keep their values in the VS Code server and its terminals (user decision). Git does not use them: the settings of the container switch the credential helpers of Git and Docker off, `GIT_SSH_COMMAND` switches the SSH agent off for Git, and Git signs nothing unless the user configures it. A process of the remote user or root that looks for them can use them: through the IPC socket it can read, store, or erase the computer's Git and Docker credentials (the computer's credential helpers decide what `git credential fill` returns), and through the agents it can use the SSH and GPG keys of the computer. The settings of the container are in `~/.vscode-server/data/Machine/settings.json`, which the Dev Containers extension writes only at the first attach of a new container: a process in the container can change or remove the file (then the user's settings apply again; the variables of Git and Docker still keep Git and Docker on the configuration of the volume, except for Git older than 2.31 and processes without the variables), and a `~/.vscode-server` on a volume keeps the file of an earlier container. The VS Code remote API (clipboard, `openExternal`, the `code` command, commands of local extensions, sign-in requests with consent, Git askpass) stays available too. Full isolation needs a separate macOS user, a VM, or Docker Desktop Enhanced Container Isolation.
- **Ports on `127.0.0.1` are reachable from every container.** Binding published ports to `127.0.0.1` protects against the network only: every container reaches `localhost` of the computer through `host.docker.internal`, also the ports that VS Code forwards for another environment, including one of another GitHub account, and services of the computer that listen on `127.0.0.1`. Network access to the computer is allowed (user decision), so the extension does not block it; a service that other containers must not reach needs its own authentication.
- **`--network host` on Linux with Docker Engine shares more than ports.** The container joins the network namespace of the computer. Besides its ports and the services on `127.0.0.1`, it reaches the abstract Unix sockets of the computer, which have no file and need no mount: for example the X11 display of the desktop (`@/tmp/.X11-unix/X0`), some D-Bus buses, and the sockets of other local programs. Most of them check who connects (the X11 cookie, the user ID for D-Bus), but a program that accepts any local connection can be used from the container. `--network host` is allowed (user decision); a repository that uses it has this access. With Docker Desktop, the host network is the network of its virtual machine, not of the computer.
- **Configurations that take rights away.** With `--cap-drop` of the file rights of root (for example `ALL` or `DAC_OVERRIDE`), root in the container may not change files of the user. The extension then writes `~/.gitconfig` as the remote user (the token is removed by the workspace helper, which such a configuration does not affect); the change of owner after `up` fails, which is logged. With `--read-only` and no writable home folder (a volume or `--tmpfs`), VS Code Server cannot be installed.
- **`--env-file` follows links, and its content is not checked.** The check allows only a path below `/workspaces`, but a symbolic link of the repository at that path can point to another file of the workspace helper during `up` (its image, or the cache volume `devenv-helper-cache`). The file can change until the container starts, so its variables are not checked. Docker applies the `-e` values after those of the file, so the variables of the override configuration's `containerEnv` keep their values; the file can still set the other variables of Git's configuration (for example `GIT_CONFIG_PARAMETERS`, which Git applies after `GIT_CONFIG_COUNT`), for all processes of the container, also the VS Code server and its terminals.
- **The machine settings file of VS Code Server.** The settings of the container (the switches of the Dev Containers features, section 5, and the refusal of `remote.localPortHost`, section 1) reach `~/.vscode-server/data/Machine/settings.json` only when the Dev Containers extension creates that file. An image that ships its own file keeps its values, and any process in the container can change the file. Such a file can set `remote.localPortHost` to `allInterfaces`, so that VS Code forwards the ports on all addresses of the computer, and it can leave out the settings that switch the Git and Docker credential forwarding off (Git in the container still uses only its own configuration, section 5). The extension does not read this file.
- **Volumes of a failed first open.** When the first open of a repository fails after `up` created the container, the extension removes the environment (its container, images, workspace volume, and entry). The additional volumes that it recorded (for example `${devcontainerId}-history`, or a volume of the configuration) stay, and no Delete lists them; `docker volume rm` removes them. The registry keeps them with the account, as after a Delete that kept them.
- **Named volumes without labels.** The check knows the volumes of Dev Environments, of the Dev Containers extension, of the environments of other accounts, and the existing volumes that Docker Compose, the Dev Containers extension, or Docker labelled. A volume without such labels, for example one created with `docker volume create`, can be mounted by any configuration that knows its name. The volumes of the environments of other accounts are known from the registry, also the volumes that a Delete kept (with the account whose environment used them; a volume that the Deletes of two accounts kept is refused to both; the records of a volume that no longer exists are dropped when a configuration mounts its name, because a new volume of that name is empty). After a loss of the registry, the records of kept volumes are lost with it and cannot be restored (the Delete removed the containers that the restore reads): the environment of any account can then mount such a volume; `docker volume rm` removes it. Otherwise, after a loss of the registry, the restore reads them from the containers of the environments, which a loss of the registry does not remove (their named volumes, without the volumes that the policy gives to something else by their names or labels: anonymous volumes, volumes of the Dev Containers extension, of the helper, of other environments, and of other programs); an environment whose container is gone too has only its workspace volume known, and the account whose environment of the repository opens first then records a fixed-name volume of the repository as its own, so that the environment of the other account is refused it.
- **Repository code runs, with the network.** Dockerfile `RUN`, Feature install scripts and entrypoints, and lifecycle commands run. A container is not a strong security boundary.
- **Old Git in the image.** Git older than 2.32 ignores `GIT_CONFIG_GLOBAL`, and Git older than 2.31 also `GIT_CONFIG_COUNT`: it reads the volume's configuration (before 2.31 also the credential helper of the container) only through the extension's `~/.gitconfig`, which is missing when the image brings its own file with content. With the settings of the container, the Dev Containers extension configures no forwarding helper; without them (a changed settings file), such Git may use the computer's credential helper for hosts other than github.com, and Git older than 2.9, which does not clear helpers with an empty value, also for github.com; the extension checks the Git version of each new container and warns about Git older than 2.9. `sudo git` and `env -i git` also drop the variables.
- **The token stays after sign-out.** When a window leaves an environment because of a sign-out or an account change, the workspace helper removes the token file and the sign-in of the GitHub CLI (`gh/hosts.yml`) from the volume of the environment, whether its container runs or not. Otherwise (an environment that no window of this computer shows) both stay until the next open or Delete. Only revoking VS Code's access on GitHub makes it invalid.
- **Not restricted yet:** X11 forwarding (XQuartz; the Dev Containers extension forwards it unless the environment of the container has a non-empty `DISPLAY`; `DISPLAY=''` in `remoteEnv` does not stop it), the copy of `~/.ssh/known_hosts` (at each attach, when the container has no such file; an empty file would stop it), and the user's dotfiles (cloned and installed in each new container; they cannot be switched off for one container).

## 11. Known gaps

Found when this summary was verified against the code. Gaps 1 to 7 and 9 are fixed or documented; gap 8 is resolved by one environment per repository and account.

1. **Fixed: the `runArgs` check could be bypassed.** The check runs on the repository's `runArgs`, but `docker run` gets them after the removal of `--name` (which also removes the next entry, even when `--name` is the value of another flag) and after entries that are not strings are dropped. `["--label","--name","--init","--label","-v/Users:/host"]` passes and becomes a bind mount. Fixed: entries that are not strings are refused, `--name` is removed with the same flag parser as the check, and the final `runArgs` are checked before `up`.
2. **Fixed: the empty `~/.gitconfig` did not stop the copy.** The Dev Containers extension skips the copy only when the file has a section other than `[filter]` or `[safe]`, so the computer's `~/.gitconfig` is appended at the first attach. Fixed with the documented setting `copyGitConfig: false` for the container (gap 3, section 5); the extension relies on no internal check of the Dev Containers extension (user decision).
3. **Fixed: `REMOTE_CONTAINERS_IPC` carries credentials, not the browser.** In Dev Containers 0.470.0 the socket answers only the requests of its forwarding credential helpers of Git and Docker, with the computer's credentials (read, store, and erase); `$BROWSER`, the `code` command, `openExternal`, Open in Browser, and port forwarding use the channels of VS Code itself. Fixed only with the documented settings of the Dev Containers extension for the container (`copyGitConfig` under both keys, `gitCredentialHelperConfigLocation: none`, `dockerCredentialHelper: false`, `githubCLILoginWithToken: false`): Git and Docker in the container do not use the socket. By user decision, no variable of the Dev Containers extension or the VS Code server is set or changed, so `REMOTE_CONTAINERS_IPC`, `SSH_AUTH_SOCK`, and `$BROWSER` keep their values, and the extension no longer writes the placeholder that kept the GPG agent out. Containers of an older version are created again (container version 3; version 4 adds `GH_CONFIG_DIR`). The sockets stay usable by a process that looks for them (section 10). To verify in real VS Code (V-8).
4. **Fixed: other named volumes could be mounted** (for example the Dev Containers `vscode` volume); only `devenv-*` names were protected. Rule: a mount may use anonymous volumes, tmpfs, the own workspace volume, and named volumes of the repository, but no volume that belongs to something else: another environment (by its name, or used by an environment of another account), another container (by the name of an anonymous volume), the workspace helper, the Dev Containers extension (by its names), or another program (by the labels of an existing volume). See section 1; volumes without labels stay a limit (section 10). With one environment per repository and account, a fixed volume name of a repository used by the environment of another account is refused by the same rule.
5. **Fixed: a repository label could replace the id label.** A repository `--label devenv.environment-id=…` won over the CLI's id label. Labels with the prefixes `devenv.` and `devcontainer.` in `runArgs` are refused (section 1).
6. **Fixed: `-e`/`--env-file` in `runArgs` could override the Git variables** (`GIT_CONFIG_*`, `DOCKER_CONFIG`, `GNUPGHOME`, `GIT_SSH_COMMAND`, `SSH_AUTH_SOCK`) for the container's main process and plain `docker exec`. The variables that the extension sets (`GIT_CONFIG_GLOBAL`, `GIT_CONFIG_COUNT`/`KEY_n`/`VALUE_n`, `DOCKER_CONFIG`, `GIT_SSH_COMMAND`, `GH_CONFIG_DIR`) and every other `GIT_CONFIG*` variable are refused in `-e`/`--env`, `containerEnv`, and `remoteEnv`, also of Features and the base image (section 1). `GNUPGHOME` and `SSH_AUTH_SOCK` are no longer set by the extension (user decision), so there is nothing of it to override. `--env-file` stays allowed below `/workspaces`: Docker applies `-e` after the file, so the extension's own values win; the other variables of the file are a documented limit (section 10).
7. **Documented: ports on `127.0.0.1` are reachable from every container** through `host.docker.internal`, including ports that VS Code forwards for another account's environment. Binding to localhost protects against the LAN only. A known limit (section 10), because network access to the computer is allowed.
8. **The row "Environment of another account" and the `otherAccount` message reveal** that another account has an environment of a repository that the signed-in account can access. Resolved by one environment per repository and account: the row is gone, and `otherAccount` names only an environment that a command names by its ID (a row or the status bar item from before an account change, a restored window, an account change during an open, and an account change during Switch branch…: the environment whose branch it switched, also after a repository pick).
9. **Fixed for the configuration: a configuration could make VS Code forward ports on all addresses of the computer** (found with gap 3): `remote.localPortHost: allInterfaces` in `customizations.vscode.settings` becomes a setting of the container, which the window applies. Refused (section 1). An image or a process in the container can still write the setting into the machine settings file of VS Code Server (a known limit, section 10).
