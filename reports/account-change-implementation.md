# Implementer's report — account isolation, container-only Git, host access policy (uncommitted, base 8cdcca4)

Verification by the implementer: tsc 0 errors; vitest 56 files / 1555 tests (now 1567 after a partial review of hostAccess.ts); production build OK; VSIX OK; `npm run test:docker` 25/25 passed BEFORE Docker Desktop was uninstalled on the machine (~09:37). Docker Desktop is currently NOT installed on this Mac (the app is gone; its data Docker.raw is intact) — Docker tests cannot run now.

New files: src/core/ownership.ts, src/core/helper/hostAccess.ts, src/core/helper/containerGit.ts (+ tests). Changed: core types/names/messages/errors/ports, storage paths/registry, discovery/discoveryService, helper scripts/devcontainerCli/workspaceHelper/configChecks/localEnv, pipeline environmentService/pipelineRules/testkit; vscode auth/errors/extension/sidebar/controller/treeModel/activationRules; test/docker harness/dockerRun/pipeline.test; docs (concept, notes, README).

How it works (summary):
- Owner {id, login} on Environment, volume label devenv.owner-id; GitHubAuth.getAccount; isAvailableTo/availableEnvironments; EnvironmentClaims for legacy entries via getRepository (serialized, under registry lock); filtering in sidebar/search/switcher/pickers/all commands/reopen/pending operations/disconnect requests; restored window of another account: no pipeline, window status cleared, message, Close Remote Connection; account change closes the connection; repositories-<accountId>.json, old repositories.json deleted; refresh refuses to store when the token's user id differs.
- Container Git: a helper run at every open (no Docker socket, no network, token on stdin via tmpfs) writes /workspaces/.devenv+/github-token (0600, uid:gid of the repo folder, atomic rename), gitconfig (identity from viewer{databaseId login name}; existing file: only the credential section ensured), docker/ and gnupg/ (0700), gnupg/private-keys-v1.d/README-devenv; symlinks replaced. containerEnv+remoteEnv: GIT_CONFIG_GLOBAL, DOCKER_CONFIG, GNUPGHOME, GIT_SSH_COMMAND; remoteEnv SSH_AUTH_SOCK=''. Deviation: GIT_CONFIG_COUNT=2 with credential.helper= and credential.https://github.com.helper=<ours> (command-line level), because Dev Containers writes its forwarding helper with --system and --global. Empty ~/.gitconfig and ~/.config/git/config created with docker exec -u root after container creation. Label devenv.container-version=2; older containers recreated from the environment image (up --remove-existing-container, no build).
- Host policy (hostAccess.ts, table-driven): checked on resolved + mergedConfiguration (read-configuration --include-merged-configuration, fallback without) before any build, and on the image's devcontainer.metadata label before each up that creates a container. Refused → code hostAccess, volume kept, a refused new image of an update removed, old container kept. -p without address and appPort bound to 127.0.0.1. ${localEnv} no longer passed (warning). initializeCommand refused. Extra rules: hostRequirements.gpu refused, volume-driver/volume-opt refused, --build-context docker-image:// and http(s):// allowed. Strict allow-list: unknown runArgs flags refused (incl. --platform, --rm, --tmpfs, --cap-drop, --read-only, -it).
- BROWSER, forwardPorts, portsAttributes untouched; no network restriction; --network host allowed.
- Deviations: ${localEnv} HOME/PATH/HOSTNAME resolve to helper values; owner login empty after restore from label until next open; a second account cannot create its own environment for a repository with another account's hidden environment (D-3).
- Open points (V-8/V-7): Dev Containers applies remoteEnv from the label and does not override the variables; git push from SCM view/terminal uses the container helper; empty ~/.gitconfig stops the copy; placeholder stops GPG forwarding; restored window of another account may briefly connect before closing; sign-out closes all connected windows; VPN on Windows/Linux; Git < 2.32 ignores GIT_CONFIG_GLOBAL; any account can claim an old public repository's environment; Dev Containers setting githubCLILoginWithToken would log gh in with the Mac token; environments using docker-in-docker / Docker socket Feature / bind mounts are now refused.

Texts added (messages.ts): hostAccess, localEnvNotPassed, otherAccount, otherAccountConnection, gitSetupFailed, placeholder file content.
