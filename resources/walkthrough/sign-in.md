# Sign in with GitHub

Dev Environments lists your GitHub repositories that have a Dev Container configuration.

**Sign in with GitHub** uses the GitHub sign-in of VS Code. Then select the **Dev Environments** icon in the activity bar and **Start** a repository.
